sudo: required
services:
  - docker

before_install:
  - docker build -t preetiprakash/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true preetiprakash/react-test npm test -- --coverage

after_success:
  - docker build -t preetiprakash/multi-client ./client
  - docker build -t preetiprakash/multi-nginx ./nginx
  - docker build -t preetiprakash/multi-server ./server
  - docker build -t preetiprakash/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push preetiprakash/multi-client
  - docker push preetiprakash/multi-nginx
  - docker push preetiprakash/multi-server
  - docker push preetiprakash/multi-worker